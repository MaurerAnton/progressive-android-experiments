#pragma once
#include <string>
#include <vector>
#include <cstdint>
#include <sstream>
std::string parseGeoUri:buildLocationEvent:calculateDistance:isValidGeoCoordinates:renderStaticMapUrl(const std::string& json);
