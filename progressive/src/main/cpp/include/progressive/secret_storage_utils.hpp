#pragma once
#include <string>
#include <vector>
#include <cstdint>
#include <sstream>
std::string encryptSecret:decryptSecret:parseSecretStorageKey:buildSecretStorageEvent:getDefaultKeyId(const std::string& json);
